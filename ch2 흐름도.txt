ch2 흐름도 
문제 정의
→ 데이터 수집
→ EDA
→ 특성 엔지니어링
→ 모델 선택
→ 학습

→ 평가

1_1. 문제 정의 : 구역 데이터들을 통한 구역 가격 결정
모델을 통해 산출된 구역 가격을 통해 투자 가치를 결정한다.
예측에 사용될 특성이 여러 개인 다중 회귀 문제이자 구역마다 하나의 값을 예측하기에 단변량 회귀 문제이다.
마지막으로 해당 시스템으로 들어오는 데이터에 연속적인 흐름이 없으므로 빠르게 변하는 데이터에 적응하지 않아도 되고, 
데이터가 메모리에 들어갈 만큼 충분히 작으므로 일반적인 배치 학습이 적절하다.

1_2 성능 측정 지표 선택 : RMSE 평균 제곱근 오차
(이상치 다량 존재 시 MAE 평균 절대 오차 고려 가능
종 모양 분포의 양 끝단처럼 이상치가 매우 드물면 RMSE가 잘 맞는다.

# RMSE와 MAE 모두 예측값의 벡터와 타깃값의 벡터 사이의 거리 재는 방법으로 RMSE는 유클리드 노름
L2 노름을 사용한다.

1_3 가정 검사 : 혹여 '저렴', '보통', '고가' 같은 카테고리값(분류 문제)이 필요한 것이 아닌지,, etc


2_1 데이터 가져오기 및 다운로드 

2_2 데이터 구조 훑어보기 : 전체 행 수, 각 특성의 데이터 타입과 널이 아닌 값의 개수, 백분위수 등 확인

2_3 테스트 세트 만들기 : 1. 초깃값을 지정한 난수 발생기 -> 데이터셋 추가시 문제 발생 2. 일반적인 랜덤 샘플링 방법 3. 사이킷런 함수를 이용한 계층적 샘플링


3_1 데이터 이해를 위한  시각화 : 산점도, 히스토그램 등을 통해 군집이나 패턴등을 파악

3_2 데이터 이해를 위한 탐색 : 모든 특성 간의 표준 상관계수(피어슨의 r)통해 상관관계 조사'

3_3 특성 조합으로 실험 : 프로토타입을 만들고 실행한 후 결과 분석해 더 많은 인사이트 얻고 해당 탐색 단계로 다시 돌아온다.

-> 3번 과정에서 이상한 형태의 데이터 제거, 정제 가능


4_1 데이터 정제 : 해당 구역 삭제, 해당 특성 삭제, 대체를 통해 누락된 특성 처리 

4_2 텍스트와 범주형 특성 다루기 : 사이킷런 클래스를 이용해 카테고리를 텍스트에서 숫자로 혹은 출력이 희소 행렬인 원핫 인코딩으로 

-> 데이터 준비 과정